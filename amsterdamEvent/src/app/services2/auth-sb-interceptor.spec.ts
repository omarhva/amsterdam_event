import { AuthSbInterceptor } from './auth-sb-interceptor';

describe('AuthSbInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthSbInterceptor()).toBeTruthy();
  });
});
